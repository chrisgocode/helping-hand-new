# Helping Hand

```text
apps/
  api/       Hono + Cloudflare Workers
  mobile/    reserved until the Meta DAT mobile platform is chosen
  web/       React + Vite
packages/
  api-client/
  db/
  schemas/
```

```sh
bun install
bun run dev
```

Run one app with `bun run dev:web` or `bun run dev:api`.
